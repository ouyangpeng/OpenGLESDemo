<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.oyp.openglesdemo">

    <uses-feature android:glEsVersion="0x00030000"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OpenGLESDemo">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".NativeRenderActivity"
            android:label="@string/native_renderer"/>

        <activity
            android:name=".lesson.LessonThreeActivity"
            android:label="@string/lesson_three"/>

        <activity
            android:name=".lesson.LessonFourActivity"
            android:label="@string/lesson_four"/>


        <activity
            android:name=".lesson.lesson5.LessonFiveActivity"
            android:label="@string/lesson_five"/>

        <activity
            android:name=".lesson.lesson6.LessonSixActivity"
            android:label="@string/lesson_six"/>

        <activity
            android:name=".lesson.lesson7.LessonSevenActivity"
            android:label="@string/lesson_seven"/>

        <activity
            android:name=".lesson.lesson8.LessonEightActivity"
            android:label="@string/lesson_eight"/>
    </application>

</manifest>